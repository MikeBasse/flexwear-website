/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Ready for future remote image/CMS integration
    remotePatterns: [],
  },
};

export default nextConfig;
